Mongo
-----
database: Budgeto
user: endless
pass: une*cesttrop

database: admin
user: admin
pass: monterdelaid
----

db.auth("endless", "une*cesttrop")
db.auth("admin", "monterdelaid")

sudo mongodump --out /tmp/mongo/backup --username endless --password une*cesttrop --db Budgeto
sudo mongorestore Budgeto/ --username <user> --password <password> --db Budgeto

db.user.insert({"languageID":1, "firstName": "Bob", "lastName": "Alice", "email": "BoblikesAlice@gmail.com"})

db.createUser({user: "admin", pwd: "monterdelaid", roles: [ { role: "userAdminAnyDatabase", db: "admin" } ] })
db.createUser({user: "endless", pwd: "une*cesttrop", roles: [ { role: "readWrite", db: "Budgeto" } ] })

auth = true